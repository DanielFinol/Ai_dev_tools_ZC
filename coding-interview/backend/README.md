# Coding Interview Backend

This folder contains a minimal Express API to create interview sessions and a small wrapper to run a `y-websocket` server for Yjs realtime collaboration.

Run the API:

```bash
# from coding-interview/backend
node server.js
```

Run the websocket server:

```bash
node ws-server.js
```
